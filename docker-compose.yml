version: '3.8'

services:
  phishguard:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: phishguard-ai
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
